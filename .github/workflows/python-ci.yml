name: Python CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    name: Test on Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Install the package so tests can import `lumina`
          pip install -e .

      - name: Install and run style checks (isort/black/ruff)
        run: |
          python -m pip install --upgrade pip
          pip install isort black ruff
          # Run check-only versions so CI doesn't auto-fix files
          isort --check-only .
          black --check .
          ruff check .

      - name: Lint with ruff (redundant check)
        run: |
          pip install ruff
          ruff check src tests

      - name: Run tests with coverage
        env:
          CI: true
        run: |
          pip install coverage pytest pytest-cov
          pytest -q --cov=lumina --cov-report=xml:coverage.xml

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-xml-${{ github.job }}-${{ github.run_number }}
          path: coverage.xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: coverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true
